{
  "private": true,
  "scripts": {
    "dev": "npm --prefix my-projects-site run dev",
    "build": "npm --prefix my-projects-site run build",
    "preview": "npm --prefix my-projects-site run preview"
  }
}